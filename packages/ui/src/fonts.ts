// Ligma type stack — all OFL / MIT-compatible.
// Fraunces: editorial serif display
// Inter: self-hosted neutral sans for body / UI (primary sans)
// Geist: kept as fallback in the sans stack for back-compat
// JetBrains Mono: monospaced with tabular numerals for cost/code readout
import '@fontsource-variable/fraunces';
import '@fontsource-variable/geist';
import '@fontsource-variable/inter';
import '@fontsource-variable/jetbrains-mono';
