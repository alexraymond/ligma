# @ligma/deez

**D**esign **E**xecution **E**nvironment **Z**ones.

Reserved workspace for future sandbox topology work — per-artifact execution zones with isolated module graphs. Not wired into `apps/desktop` yet; the package exists so the naming doesn't collide when we get there.

No exports. No runtime. Nothing to see here.
