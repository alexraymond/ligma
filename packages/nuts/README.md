# @ligma/nuts

**N**etwork **U**tility **T**ool **S**et.

Reserved workspace for future provider-diagnostic tooling — DNS probes, TLS handshake traces, proxy round-trip sampling. Currently empty; the namespace is claimed so the eventual implementation has a home.

No exports. No runtime. Nothing to see here.
