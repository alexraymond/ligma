{
  "name": "@ligma/cli",
  "version": "0.2.0",
  "license": "MIT",
  "private": true,
  "bin": {
    "ligma": "./bin/ligma.mjs"
  },
  "files": ["bin", "src", "package.json"],
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "@ligma/api": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "tsx": "^4.21.0",
    "typescript": "^5.8.0",
    "vitest": "^4.0.18"
  },
  "engines": {
    "node": ">=22"
  }
}
